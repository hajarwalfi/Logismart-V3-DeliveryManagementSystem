# Shared Module

This module contains reusable components, directives, and pipes that can be imported by feature modules.

## Structure:
- **components/**: Reusable UI components (buttons, cards, modals, etc.)
- **directives/**: Custom directives
- **pipes/**: Custom pipes
- **models/**: Shared data models and interfaces